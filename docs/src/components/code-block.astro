---
import { Code } from 'astro:components';

interface Props {
  code: string;
  lang?: 'tsx' | 'bash' | 'css' | 'js';
}

const { code, lang = 'tsx' } = Astro.props;
---

<Code code={code} lang={lang} theme="css-variables" />
