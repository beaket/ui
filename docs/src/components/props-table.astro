---
interface PropInfo {
  name: string;
  type: string;
  required: boolean;
  defaultValue: string | null;
  description: string;
}

interface Props {
  props: PropInfo[];
}

const { props } = Astro.props;
---

{props.length === 0 ? (
  <p class="text-sm text-[var(--steel)]">—</p>
) : (
  <table class="w-full font-mono text-xs">
    <thead>
      <tr class="border-b border-[var(--chrome)] text-left tracking-wide text-[var(--steel)] uppercase">
        <th class="py-1.5 pr-3 font-medium">Name</th>
        <th class="py-1.5 pr-3 font-medium">Type</th>
        <th class="py-1.5 pr-3 font-medium">Default</th>
        <th class="py-1.5 font-medium">Description</th>
      </tr>
    </thead>
    <tbody class="text-[13px]">
      {props.map((p) => (
        <tr class="border-b border-[var(--chrome)]">
          <td class="py-1.5 pr-3">
            {p.name}
            {p.required && <span class="text-[var(--signal-red)]">*</span>}
          </td>
          <td class="py-1.5 pr-3 text-[var(--steel)]">{p.type}</td>
          <td class="py-1.5 pr-3">{p.defaultValue ?? '—'}</td>
          <td class="py-1.5 font-sans text-[var(--steel)]">{p.description}</td>
        </tr>
      ))}
    </tbody>
  </table>
)}
