---
import Layout from '../layouts/doc.astro';
---

<Layout title="Design Rules">

<h1>Design Rules</h1>

<p>Beaket UI is an Ink & Instrument design system — a printed page you can press. These rules explain why things look the way they do — and what to preserve when you customize.</p>

<h2>No decoration</h2>

<p>No gradients. No border-radius (except Radio). No blur shadows. No opacity for styling. If it's a border, show the border. If it's a shadow, make it structural.</p>

<h2>Shadows signal state</h2>

<p>Offset shadows are not decoration — they speak. Pressables carry a thin accent edge: hover grows it, pressing drops the control onto it, disabled removes it. Instruments (small controls, fused strips) hold the edge static while the inner key travels under the press. Writing fields draw it only while focused (cap-off). Surfaces keep a quiet grey shade. This is the system's primary state language.</p>

<h2>One pattern, everywhere</h2>

<p>Same keyboard focus outline on every pressable: <code>outline-2 outline-border-focus outline-offset-2</code> — writing fields signal focus with the accent edge instead (cap-off). Same disabled style everywhere: <code>border-dashed border-border-muted bg-bg-disabled text-fg-disabled</code>. Users learn the system once and trust it completely.</p>

<h2>Own it, keep it right</h2>

<p>You own the code after installation. Every component accepts <code>className</code> — customize freely. But preserve touch targets (44px+), keyboard navigation, and ARIA attributes. The invisible quality matters most.</p>

</Layout>
