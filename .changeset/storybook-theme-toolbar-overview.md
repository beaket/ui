---
---

Storybook dev-tooling only: add a Theme/Scheme palette toolbar to the preview and a hand-authored "Overview" kitchen-sink story that renders every component on one page for theme QA. No published package changes — empty changeset (no release).
