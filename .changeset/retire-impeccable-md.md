---
---

Establish the Impeccable context artifacts and retire the legacy `.impeccable.md`.

Visual doctrine now lives in `DESIGN.md` (with the `.impeccable/design.json` sidecar) and product truth in `PRODUCT.md`; `CLAUDE.md` § Design Rules is reduced to a pointer. `@beaket/paper` gets its own `packages/paper/PRODUCT.md` so it no longer inherits a record that disclaims it. Docs and conventions only — no component, CLI, editor, or registry change.
