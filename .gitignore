.DS_Store
node_modules
dist
test-results

# Environment
.npmrc
# but track the repo-root .npmrc (provenance=true); package/home ones stay ignored
!/.npmrc
.env

# Astro
.astro

# Storybook
*storybook.log
storybook-static

# Claude
.claude/

# design-sync (claude.ai/design): generated + verification state.
# Durable, committed: config.json, NOTES.md, conventions.md, previews/, overrides/
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.design-sync/*.log
.ds-sync/
ds-bundle/

# impeccable: PRODUCT.md / DESIGN.md / .impeccable/design.json are durable and committed.
# Per-developer only: hook consent + private rule suppressions.
.impeccable/config.local.json
